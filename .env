# Configuração do Provedor de IA
# Opções: 'gemini' (padrão) ou 'openai'
AI_PROVIDER=gemini

# Variável de ambiente para a API do Google Gemini
# Se não for fornecida, o sistema usará a chave padrão incluída no código
GEMINI_API_KEY=sua_chave_api_aqui

# Variável de ambiente para a API da OpenAI (opcional)
# Necessária apenas se AI_PROVIDER=openai
OPENAI_API_KEY=sua_chave_openai_aqui

# Configurações do servidor
# Porta padrão é 5000, mas você pode alterá-la se necessário
PORT=5000

# URL do servidor Spring Boot para integração
# Exemplo: http://localhost:8080/api
SPRING_API_URL=http://sua-url-spring-aqui/api